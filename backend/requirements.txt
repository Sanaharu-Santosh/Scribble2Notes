fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
pydantic>=2.8,<3
pydantic-settings>=2.4,<3
python-multipart>=0.0.9
pillow>=10.4
# Mode 2's default engine is classical CV — no model weights, no GPU.
opencv-python-headless>=4.10
numpy>=1.26

# Export. WeasyPrint is not pure Python — it needs pango/cairo system libraries
# and at least one font installed. The Dockerfile apt-installs them; on a bare
# machine see https://doc.courtbouillon.org/weasyprint/stable/first_steps.html
python-docx>=1.1
weasyprint>=62
